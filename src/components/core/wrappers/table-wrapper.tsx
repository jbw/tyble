import styled, { StyledComponentClass } from 'styled-components';

const TableWrapper: StyledComponentClass<any, any> = styled.div`
    display: flex;
    flex-flow: column nowrap;
    flex: 1 1 auto;
`;

export default TableWrapper;
